[tool.uv]
dev-dependencies = [
    "lintro==0.24.1",
]

[tool.lintro]
exclude = [
    "_site/",
    "**/vendor/",
    "vendor/",
    ".pytest_cache/",
    "node_modules/",
    "coverage/",
    "dist/",
    "playwright-report/",
    "test-results/",
    ".lintro/",
    "**/.build/",
]

[tool.lintro.tool.hadolint]
exclude = ["_site/Dockerfile"]

[tool.lintro.tool.actionlint]
exclude = ["**/vendor/**"]
