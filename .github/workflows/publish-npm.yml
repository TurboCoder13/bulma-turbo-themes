---
name: Publish - npm

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to publish (e.g., v0.11.0)'
        required: true
        type: string
        default: 'latest'
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: read
  actions: write
  id-token: write

concurrency:
  group: publish-npm-${{ github.ref }}
  cancel-in-progress: false

jobs:
  sbom:
    name: 📋 Generate & Sign SBOM
    # Uses trust-and-skip pattern: tag already validated in PR workflow
    uses: ./.github/workflows/reusable-sbom.yml
    permissions:
      contents: read
      actions: write
      id-token: write

  build:
    name: 🔨 Build npm Package
    needs: [sbom]
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    permissions:
      contents: read
    outputs:
      package-version: ${{ steps.pkg-info.outputs.version }}
      package-name: ${{ steps.pkg-info.outputs.name }}
    steps:
      - name: Harden Runner
        if: ${{ !env.ACT }}
        uses: step-security/harden-runner@20cf305ff2072d973412fa9b1e3a4f227bda3c76 # v2.14.0
        with:
          egress-policy: block
          allowed-endpoints: >
            api.github.com:443
            bun.sh:443
            codeload.github.com:443
            github.com:443
            npmjs.org:443
            objects.githubusercontent.com:443
            pipelines.actions.githubusercontent.com:443
            registry.npmjs.org:443
            release-assets.githubusercontent.com:443

      - name: Checkout
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: Setup Bun
        uses: oven-sh/setup-bun@735343b667d3e6f658f44d0eca948eb6282f2b76 # v2.0.2
        with:
          bun-version: '1.3.5'

      - name: Setup Node.js
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f # v6.1.0
        with:
          node-version: '22'

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build package
        run: bun run build

      - name: Build theme CSS
        run: bun run build:themes

      - name: Extract package information
        id: pkg-info
        run: |
          echo "version=$(jq -r '.version' package.json)" >> "$GITHUB_OUTPUT"
          echo "name=$(jq -r '.name' package.json)" >> "$GITHUB_OUTPUT"

      - name: Run tests
        run: bun run test

      - name: Generate SHA256 checksum
        id: checksum
        run: |
          npm pack --dry-run 2>&1 | tee pack-output.txt
          # Use tail -1 to get only the tarball name, ignoring prepare script output
          TARBALL=$(npm pack 2>/dev/null | tail -1)
          SHA256=$(sha256sum "$TARBALL" | cut -d' ' -f1)
          echo "sha256=$SHA256" >> "$GITHUB_OUTPUT"
          echo "tarball=$TARBALL" >> "$GITHUB_OUTPUT"

      - name: Upload package artifact
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: npm-package-${{ steps.pkg-info.outputs.version }}
          path: '*.tgz'
          retention-days: 90
          if-no-files-found: error

      - name: Build summary
        run: |
          {
            echo "## 📦 npm Package Built"
            echo ""
            echo "| Property | Value |"
            echo "|----------|-------|"
            echo "| Package | \`${{ steps.pkg-info.outputs.name }}\` |"
            echo "| Version | \`${{ steps.pkg-info.outputs.version }}\` |"
            echo "| SHA256 | \`${{ steps.checksum.outputs.sha256 }}\` |"
          } >> "$GITHUB_STEP_SUMMARY"

  publish:
    name: 📦 Publish to npm
    needs: [build]
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
      id-token: write
    environment:
      name: npm
      url: https://www.npmjs.com/package/${{ needs.build.outputs.package-name }}
    steps:
      - name: Harden Runner
        if: ${{ !env.ACT }}
        uses: step-security/harden-runner@20cf305ff2072d973412fa9b1e3a4f227bda3c76 # v2.14.0
        with:
          egress-policy: block
          allowed-endpoints: >
            api.github.com:443
            codeload.github.com:443
            github.com:443
            objects.githubusercontent.com:443
            registry.npmjs.org:443
            fulcio.sigstore.dev:443
            rekor.sigstore.dev:443
            tuf-repo-cdn.sigstore.dev:443

      - name: Checkout
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: Download package artifact
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: npm-package-${{ needs.build.outputs.package-version }}
          path: .

      - name: Setup Node.js for npm publish
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f # v6.1.0
        with:
          node-version: '24'
          registry-url: https://registry.npmjs.org

      - name: Publish to npm (OIDC)
        run: npm publish ./*.tgz --access public --provenance

      - name: Summary
        run: |
          {
            echo "## 🚀 Published to npm"
            echo ""
            echo "| Property | Value |"
            echo "|----------|-------|"
            echo "| Package | [${{ needs.build.outputs.package-name }}](https://www.npmjs.com/package/${{ needs.build.outputs.package-name }}) |"
            echo "| Version | \`${{ needs.build.outputs.package-version }}\` |"
            echo "| Registry | [npmjs.org](https://registry.npmjs.org) |"
            echo ""
            echo "### Install"
            echo "\`\`\`bash"
            echo "npm install ${{ needs.build.outputs.package-name }}@${{ needs.build.outputs.package-version }}"
            echo "\`\`\`"
          } >> "$GITHUB_STEP_SUMMARY"
