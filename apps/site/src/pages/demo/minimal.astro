---
/**
 * Minimal demo - Pure CSS with Turbo Themes.
 * Standalone page without the main site layout.
 */
const baseUrl = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<!doctype html>
<html lang="en" data-theme="catppuccin-mocha" data-baseurl={baseUrl}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Turbo Themes - Minimal Demo</title>

    <!-- Turbo Themes: Pure CSS Custom Properties (~10KB total) -->
    <link rel="stylesheet" href={`${baseUrl}/assets/css/turbo-core.css`} />
    <link rel="stylesheet" href={`${baseUrl}/assets/css/turbo-base.css`} />
    <link rel="stylesheet" href={`${baseUrl}/assets/css/turbo-syntax.css`} />
    <link id="turbo-theme-css" rel="stylesheet" href={`${baseUrl}/assets/css/themes/turbo/catppuccin-mocha.css`} />

    <!-- Minimal site-specific styles (~2KB) -->
    <style>
      .container { max-width: 900px; margin: 0 auto; padding: 0 1rem; }
      .header { background: var(--turbo-bg-surface); border-bottom: 1px solid var(--turbo-border-default); padding: 1rem 0; position: sticky; top: 0; z-index: 100; }
      .header-content { display: flex; justify-content: space-between; align-items: center; }
      .logo { font-size: 1.25rem; font-weight: 600; color: var(--turbo-brand-primary); text-decoration: none; }
      .theme-select { padding: 0.5rem 1rem; background: var(--turbo-bg-overlay); color: var(--turbo-text-primary); border: 1px solid var(--turbo-border-default); border-radius: 0.375rem; font-size: 0.875rem; cursor: pointer; }
      .theme-select:focus { outline: 2px solid var(--turbo-brand-primary); outline-offset: 2px; }
      main { padding: 2rem 0; }
      section { margin-bottom: 3rem; }
      .card { background: var(--turbo-bg-surface); border: 1px solid var(--turbo-border-default); border-radius: 0.5rem; padding: 1.5rem; margin-bottom: 1rem; }
      .card-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 1rem; }
      .swatch-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(120px, 1fr)); gap: 0.5rem; }
      .swatch { padding: 1rem; border-radius: 0.375rem; font-size: 0.75rem; text-align: center; }
      .footer { background: var(--turbo-bg-surface); border-top: 1px solid var(--turbo-border-default); padding: 2rem 0; text-align: center; color: var(--turbo-text-secondary); font-size: 0.875rem; }
      .stats { display: flex; gap: 2rem; flex-wrap: wrap; justify-content: center; margin: 2rem 0; }
      .stat { text-align: center; }
      .stat-value { font-size: 2.5rem; font-weight: 700; color: var(--turbo-brand-primary); }
      .stat-label { font-size: 0.875rem; color: var(--turbo-text-secondary); }
    </style>

    <script is:inline>
      (function() {
        var STORAGE_KEY = 'turbo-theme';
        var baseUrl = document.documentElement.getAttribute('data-baseurl') || '';
        var savedTheme = localStorage.getItem(STORAGE_KEY) || 'catppuccin-mocha';
        document.documentElement.setAttribute('data-theme', savedTheme);
        var themeLink = document.getElementById('turbo-theme-css');
        if (themeLink) themeLink.href = baseUrl + '/assets/css/themes/turbo/' + savedTheme + '.css';
      })();
    </script>
  </head>
  <body>
    <header class="header">
      <div class="container header-content">
        <a href="/" class="logo">Turbo Themes</a>
        <select class="theme-select" id="theme-selector" aria-label="Select theme">
          <option value="catppuccin-mocha">Catppuccin Mocha</option>
          <option value="catppuccin-macchiato">Catppuccin Macchiato</option>
          <option value="catppuccin-frappe">Catppuccin Frappe</option>
          <option value="catppuccin-latte">Catppuccin Latte</option>
          <option value="dracula">Dracula</option>
          <option value="github-dark">GitHub Dark</option>
          <option value="github-light">GitHub Light</option>
          <option value="bulma-dark">Bulma Dark</option>
          <option value="bulma-light">Bulma Light</option>
        </select>
      </div>
    </header>

    <main>
      <div class="container">
        <section>
          <h1>Minimal Demo</h1>
          <p>This page demonstrates Turbo Themes with <strong>zero framework dependencies</strong>. All styling comes from pure CSS Custom Properties.</p>
          <div class="stats">
            <div class="stat"><div class="stat-value">~10KB</div><div class="stat-label">Total CSS</div></div>
            <div class="stat"><div class="stat-value">~2KB</div><div class="stat-label">Per Theme</div></div>
            <div class="stat"><div class="stat-value">0</div><div class="stat-label">Dependencies</div></div>
          </div>
        </section>

        <section>
          <h2>Color Palette</h2>
          <div class="swatch-grid">
            <div class="swatch" style="background: var(--turbo-bg-base); color: var(--turbo-text-primary)">bg-base</div>
            <div class="swatch" style="background: var(--turbo-bg-surface); color: var(--turbo-text-primary)">bg-surface</div>
            <div class="swatch" style="background: var(--turbo-bg-overlay); color: var(--turbo-text-primary)">bg-overlay</div>
            <div class="swatch" style="background: var(--turbo-brand-primary); color: var(--turbo-text-inverse)">brand-primary</div>
            <div class="swatch" style="background: var(--turbo-state-success); color: var(--turbo-text-inverse)">state-success</div>
            <div class="swatch" style="background: var(--turbo-state-warning); color: var(--turbo-text-inverse)">state-warning</div>
            <div class="swatch" style="background: var(--turbo-state-danger); color: var(--turbo-text-inverse)">state-danger</div>
            <div class="swatch" style="background: var(--turbo-state-info); color: var(--turbo-text-inverse)">state-info</div>
          </div>
        </section>

        <section>
          <h2>Typography</h2>
          <h1>Heading 1</h1>
          <h2>Heading 2</h2>
          <h3>Heading 3</h3>
          <p>This is a paragraph with <strong>bold text</strong>, <em>italic text</em>, and <a href="#">a link</a>.</p>
        </section>

        <section>
          <h2>Code</h2>
          <p>Inline code: <code>const theme = 'catppuccin-mocha';</code></p>
          <pre><code>// Code block
function setTheme(themeId) {'{'}
  document.documentElement.setAttribute('data-theme', themeId);
  localStorage.setItem('turbo-theme', themeId);
{'}'}</code></pre>
        </section>

        <section>
          <h2>Cards</h2>
          <div class="card-grid">
            <div class="card"><h3>Platform Agnostic</h3><p>Works with any CSS framework or none at all.</p></div>
            <div class="card"><h3>Tiny Footprint</h3><p>~2KB per theme, ~4KB base styles.</p></div>
            <div class="card"><h3>Easy Integration</h3><p>Include CSS, set <code>data-theme</code>, done.</p></div>
          </div>
        </section>

        <section>
          <h2>Form Elements</h2>
          <form onsubmit="return false;">
            <div style="margin-bottom: 1rem">
              <label for="name" style="display: block; margin-bottom: 0.5rem">Name</label>
              <input type="text" id="name" placeholder="Enter your name" style="width: 100%; max-width: 300px" />
            </div>
            <div style="margin-bottom: 1rem">
              <label for="message" style="display: block; margin-bottom: 0.5rem">Message</label>
              <textarea id="message" rows="4" placeholder="Enter your message" style="width: 100%; max-width: 500px"></textarea>
            </div>
            <button type="submit">Submit</button>
          </form>
        </section>
      </div>
    </main>

    <footer class="footer">
      <div class="container">
        <p><strong>Turbo Themes</strong> — Platform-agnostic design tokens. <a href="/demo/">Back to demos</a></p>
      </div>
    </footer>

    <script is:inline>
      const selector = document.getElementById('theme-selector');
      const themeLink = document.getElementById('turbo-theme-css');
      const STORAGE_KEY = 'turbo-theme';
      const baseUrl = document.documentElement.getAttribute('data-baseurl') || '';
      const savedTheme = localStorage.getItem(STORAGE_KEY) || 'catppuccin-mocha';
      selector.value = savedTheme;

      selector.addEventListener('change', (e) => {
        const theme = e.target.value;
        document.documentElement.setAttribute('data-theme', theme);
        localStorage.setItem(STORAGE_KEY, theme);
        themeLink.href = baseUrl + '/assets/css/themes/turbo/' + theme + '.css';
      });
    </script>
  </body>
</html>
